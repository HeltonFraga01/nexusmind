# ============================================================================
# NexusMind — Swarm env example
# Uso:
#   cp nexusmind/deploy/stack.swarm.env.example .env.swarm   # editar valores reais
#   export $(grep -v '^#' .env.swarm | xargs)
#   docker stack deploy -c nexusmind/deploy/stack.swarm.yml nexusmind
# ============================================================================

# Imagem de runtime (obrigatória)
NEXUSMIND_IMAGE=heltonfraga/nexusmind:latest

# Swarm placement (nó stateful)
SWARM_STATEFUL_NODE=manager-1

# Database
POSTGRES_IMAGE=postgres:18-alpine
POSTGRES_USER=nexusmind
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD
POSTGRES_DB=nexusmind
DATABASE_SSLMODE=disable

# Redis
REDIS_IMAGE=redis:8-alpine
REDIS_PASSWORD=
REDIS_DB=0
REDIS_POOL_SIZE=1024
REDIS_MIN_IDLE_CONNS=10
REDIS_ENABLE_TLS=false

# Admin bootstrap (primeiro boot)
ADMIN_EMAIL=admin@nexusmind.local
ADMIN_PASSWORD=CHANGE_ME_STRONG_ADMIN_PASSWORD

# Segredos de segurança
JWT_SECRET=CHANGE_ME_LONG_RANDOM_SECRET
TOTP_ENCRYPTION_KEY=CHANGE_ME_LONG_RANDOM_HEX
JWT_EXPIRE_HOUR=24

# Server
SERVER_MODE=release
RUN_MODE=standard
TZ=America/Sao_Paulo
SERVER_PUBLISHED_PORT=8080

# Pool do banco
DATABASE_MAX_OPEN_CONNS=80
DATABASE_MAX_IDLE_CONNS=20
DATABASE_CONN_MAX_LIFETIME_MINUTES=30
DATABASE_CONN_MAX_IDLE_TIME_MINUTES=5

# Flags opcionais
SECURITY_URL_ALLOWLIST_ENABLED=false
SECURITY_URL_ALLOWLIST_ALLOW_INSECURE_HTTP=false
SECURITY_URL_ALLOWLIST_ALLOW_PRIVATE_HOSTS=false
SECURITY_URL_ALLOWLIST_UPSTREAM_HOSTS=
UPDATE_PROXY_URL=
